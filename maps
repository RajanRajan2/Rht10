import folium

# Create a map centered at a specific location
map = folium.Map(location=[latitude, longitude], zoom_start=zoom_level)

# Add markers to the map for specific locations
folium.Marker(location=[latitude1, longitude1], popup="Location 1").add_to(map)
folium.Marker(location=[latitude2, longitude2], popup="Location 2").add_to(map)
# Add more markers as needed

# Save the map to an HTML file
map.save("world_map.html")
